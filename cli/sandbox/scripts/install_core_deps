#!/usr/bin/env bash
set -euo pipefail

echo "Installing core dependencies..."
echo "  git, curl, tar, gzip, tmux, gcc, ripgrep, nodejs, npm, tree-sitter-cli"
echo

sudo dnf install -y \
  git curl tar gzip tmux gcc ripgrep nodejs npm tree-sitter-cli

echo "Core dependencies installed."
