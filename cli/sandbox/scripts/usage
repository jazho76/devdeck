#!/usr/bin/env bash
cat <<'EOF'

  devdeck sandbox - disposable fedora box for testing devdeck.
  The locally-built `devdeck` is mounted at ~/.local/bin (no prereqs baked in).

  Auxiliary commands:
    install_core_deps       # install devdeck core dependencies

  Exit the shell to destroy the container.

EOF
